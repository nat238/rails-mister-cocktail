<div class="container" style="align-content: center;">
  <div style="">
  <h1 style="color: "><%= @cocktail.name %></h1>
    <% @cocktail.doses.each do |dose| %>

      <div>
        <div><%="#{dose.ingredient.name} #{dose.description}" %></div>
      </div>
    <% end %>

  <%= link_to "add an ingredient", new_cocktail_dose_path(@cocktail), class: 'btn btn-outline-danger' %>
  <%= link_to "back", root_path, class: 'btn btn-success', style: 'color: white;' %>
  </div>
</div>
